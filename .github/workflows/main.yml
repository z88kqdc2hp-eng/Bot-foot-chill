name: Bot Auto Quotidien
on:
  schedule:
    - cron: '0 9,13,18 * * *' # Se lance seul à 10h, 14h et 19h (Heure FR)
  workflow_dispatch: # Garde le bouton pour les tests
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - run: python main.py # Lance ton script safe automatiquement
